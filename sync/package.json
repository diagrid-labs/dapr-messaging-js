{
  "name": "install-sync-examples",
  "version": "1.0.0",
  "scripts": {
    "install:mac": "(cd ./payment && npm install) & (cd ./registration && npm install)",
    "install:win": "(cd ./payment && npm install) & (cd ../registration && npm install)"
  }
}
